Robinアカウントのパスワード再設定をリクエストされました。

以下のURLから新しいパスワードを設定してください。

<%= edit_password_url(@resource, reset_password_token: @token) %>

このメールにお心当たりがない場合は、無視していただいて構いません。
パスワードは変更されません。
このリンクは発行から6時間で無効となります。
